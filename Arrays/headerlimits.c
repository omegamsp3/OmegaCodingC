#include<stdio.h>
#include<limits.h>
int main(){
     int max = INT_MIN;
    printf("Printf max. number is : %d",max);
    return 0;
} 

/* this program will give the lowest number which is currently
present in c language. */